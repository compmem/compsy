# Flanker Task

## Instructions to run

Navigate to this directory via the command prompt or terminal and then enter the following command:

`python flanker_exp.py -s mst3k`

where `mst3k` is your user id.

